// This file is auto-generated by @hey-api/openapi-ts

import type { Options as ClientOptions, TDataShape, Client } from './client';
import type { AdminAuthData, AdminAuthResponses, AdminAuthErrors, AdminLogoutData, AdminLogoutResponses, AdminGetSettingsData, AdminGetSettingsResponses, AdminGetSettingsErrors, AdminPutGithubData, AdminPutGithubResponses, AdminPutGithubErrors, AdminPutGitlabData, AdminPutGitlabResponses, AdminPutGitlabErrors, AdminDeleteCategoryData, AdminDeleteCategoryResponses, AdminDeleteCategoryErrors, AdminListLlmCredentialsData, AdminListLlmCredentialsResponses, AdminListLlmCredentialsErrors, AdminCreateLlmCredentialData, AdminCreateLlmCredentialResponses, AdminCreateLlmCredentialErrors, AdminDeleteLlmCredentialData, AdminDeleteLlmCredentialResponses, AdminDeleteLlmCredentialErrors, AdminUpdateLlmCredentialData, AdminUpdateLlmCredentialResponses, AdminUpdateLlmCredentialErrors, AdminReorderLlmCredentialsData, AdminReorderLlmCredentialsResponses, AdminReorderLlmCredentialsErrors, AdminGithubManifestData, AdminGithubManifestResponses, AdminGithubManifestErrors, AdminGithubManifestCallbackData, AdminGithubManifestCallbackResponses, AdminGithubManifestCallbackErrors, LoginGithubData, LoginGitlabData, OauthCallbackData, OauthCallbackErrors, GetMeData, GetMeResponses, GetMeErrors, UpdateProfileData, UpdateProfileResponses, UpdateProfileErrors, LogoutData, LogoutResponses, LinkProviderData, LinkProviderErrors, DisconnectProviderData, DisconnectProviderResponses, DisconnectProviderErrors, GetAppConfigData, GetAppConfigResponses, ListMcpServersData, ListMcpServersResponses, ListMcpServersErrors, CreateMcpServerData, CreateMcpServerResponses, CreateMcpServerErrors, DeleteMcpServerData, DeleteMcpServerResponses, DeleteMcpServerErrors, UpdateMcpServerData, UpdateMcpServerResponses, UpdateMcpServerErrors, DeleteCredentialData, DeleteCredentialResponses, DeleteCredentialErrors, GetCredentialData, GetCredentialResponses, GetCredentialErrors, WriteCredentialData, WriteCredentialResponses, WriteCredentialErrors, CancelExecutionData, CancelExecutionResponses, CancelExecutionErrors, ViewMemoryData, ViewMemoryResponses, ViewMemoryErrors, CreateMemoryEntryData, CreateMemoryEntryResponses, CreateMemoryEntryErrors, StrReplaceMemoryEntryData, StrReplaceMemoryEntryResponses, StrReplaceMemoryEntryErrors, InsertMemoryEntryData, InsertMemoryEntryResponses, InsertMemoryEntryErrors, DeleteMemoryEntryData, DeleteMemoryEntryResponses, DeleteMemoryEntryErrors, RenameMemoryEntryData, RenameMemoryEntryResponses, RenameMemoryEntryErrors, ListIssuesData, ListIssuesResponses, ListIssuesErrors, ListIssueRepositoriesData, ListIssueRepositoriesResponses, ListIssueRepositoriesErrors, GetIssueData, GetIssueResponses, GetIssueErrors, RetryIssueData, RetryIssueResponses, RetryIssueErrors, DismissIssueData, DismissIssueResponses, DismissIssueErrors, FixIssueData, FixIssueResponses, FixIssueErrors, ResolveIssueData, ResolveIssueResponses, ResolveIssueErrors, GetPluginsOverviewData, GetPluginsOverviewResponses, GetPluginsOverviewErrors, InstallPluginsData, InstallPluginsResponses, InstallPluginsErrors, UninstallPluginData, UninstallPluginResponses, UninstallPluginErrors, UpdateInstalledPluginData, UpdateInstalledPluginResponses, UpdateInstalledPluginErrors, ConnectMarketplaceData, ConnectMarketplaceResponses, ConnectMarketplaceErrors, DisconnectMarketplaceData, DisconnectMarketplaceResponses, DisconnectMarketplaceErrors, ListOrganizationsData, ListOrganizationsResponses, ListOrganizationsErrors, ClaimOrganizationData, ClaimOrganizationResponses, ClaimOrganizationErrors, DeleteOrganizationData, DeleteOrganizationResponses, DeleteOrganizationErrors, GetOrganizationData, GetOrganizationResponses, GetOrganizationErrors, UpdateOrganizationData, UpdateOrganizationResponses, UpdateOrganizationErrors, SyncOrganizationRepositoriesData, SyncOrganizationRepositoriesResponses, SyncOrganizationRepositoriesErrors, ListOrganizationMembersData, ListOrganizationMembersResponses, ListOrganizationMembersErrors, ListOrganizationSubgroupsData, ListOrganizationSubgroupsResponses, ListOrganizationSubgroupsErrors, GetOrganizationSettingsData, GetOrganizationSettingsResponses, GetOrganizationSettingsErrors, UpdateOrganizationSettingsData, UpdateOrganizationSettingsResponses, UpdateOrganizationSettingsErrors, GetOrganizationStatsData, GetOrganizationStatsResponses, GetOrganizationStatsErrors, ListPullRequestsData, ListPullRequestsResponses, ListPullRequestsErrors, ListPullRequestRepositoriesData, ListPullRequestRepositoriesResponses, ListPullRequestRepositoriesErrors, TriggerPullRequestWorkflowData, TriggerPullRequestWorkflowResponses, TriggerPullRequestWorkflowErrors, ListReposData, ListReposResponses, ListReposErrors, LookupReposData, LookupReposResponses, LookupReposErrors, BulkUpdateRepoSettingsData, BulkUpdateRepoSettingsResponses, BulkUpdateRepoSettingsErrors, UpdateRepoSettingsData, UpdateRepoSettingsResponses, UpdateRepoSettingsErrors, LinkRelatedRepoData, LinkRelatedRepoResponses, LinkRelatedRepoErrors, UnlinkRelatedRepoData, UnlinkRelatedRepoResponses, UnlinkRelatedRepoErrors, AddGitlabSourceData, AddGitlabSourceResponses, AddGitlabSourceErrors, LinkSentrySourceData, LinkSentrySourceResponses, LinkSentrySourceErrors, ListSourceProjectsData, ListSourceProjectsResponses, ListSourceProjectsErrors, ResolveProjectMappingsData, ResolveProjectMappingsResponses, ResolveProjectMappingsErrors, UpdateSourceProjectData, UpdateSourceProjectResponses, UpdateSourceProjectErrors, TriggerBackfillData, TriggerBackfillResponses, TriggerBackfillErrors, StreamEventsData, StreamEventsResponses, StreamEventsErrors, SentryWebhookWebhooksSentryPostData, SentryWebhookWebhooksSentryPostResponses, SentryWebhookWebhooksSentryPostErrors, GithubWebhookData, GithubWebhookResponses, GithubWebhookErrors, GitlabWebhookData, GitlabWebhookResponses, GitlabWebhookErrors, ListWorkspacesData, ListWorkspacesResponses, ListWorkspacesErrors, CreateWorkspaceData, CreateWorkspaceResponses, CreateWorkspaceErrors, FinalizeWorkspaceOnboardingData, FinalizeWorkspaceOnboardingResponses, FinalizeWorkspaceOnboardingErrors, ListWorkspaceMembersData, ListWorkspaceMembersResponses, ListWorkspaceMembersErrors, GetWorkspaceSourcesData, GetWorkspaceSourcesResponses, GetWorkspaceSourcesErrors, GetWorkspaceStatsData, GetWorkspaceStatsResponses, GetWorkspaceStatsErrors, GetWorkspaceExecutionsData, GetWorkspaceExecutionsResponses, GetWorkspaceExecutionsErrors, GetWorkspaceActiveExecutionsData, GetWorkspaceActiveExecutionsResponses, GetWorkspaceActiveExecutionsErrors, GetWorkspaceLeaderboardsData, GetWorkspaceLeaderboardsResponses, GetWorkspaceLeaderboardsErrors } from './types.gen';
import { client as _heyApiClient } from './client.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = ClientOptions<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Admin Auth
 * Exchange the admin secret for a Redis-backed session cookie.
 */
export const adminAuth = <ThrowOnError extends boolean = false>(options: Options<AdminAuthData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AdminAuthResponses, AdminAuthErrors, ThrowOnError>({
        url: '/admin/auth',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Admin Logout
 * Clear the admin session cookie and delete the Redis session.
 */
export const adminLogout = <ThrowOnError extends boolean = false>(options?: Options<AdminLogoutData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).post<AdminLogoutResponses, unknown, ThrowOnError>({
        url: '/admin/logout',
        ...options
    });
};

/**
 * Get Admin Settings
 * Return all instance settings with secrets masked.
 */
export const adminGetSettings = <ThrowOnError extends boolean = false>(options?: Options<AdminGetSettingsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<AdminGetSettingsResponses, AdminGetSettingsErrors, ThrowOnError>({
        url: '/admin/settings',
        ...options
    });
};

/**
 * Put Github Settings
 */
export const adminPutGithub = <ThrowOnError extends boolean = false>(options: Options<AdminPutGithubData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<AdminPutGithubResponses, AdminPutGithubErrors, ThrowOnError>({
        url: '/admin/settings/github',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Put Gitlab Settings
 */
export const adminPutGitlab = <ThrowOnError extends boolean = false>(options: Options<AdminPutGitlabData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<AdminPutGitlabResponses, AdminPutGitlabErrors, ThrowOnError>({
        url: '/admin/settings/gitlab',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Delete Settings Category
 */
export const adminDeleteCategory = <ThrowOnError extends boolean = false>(options: Options<AdminDeleteCategoryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<AdminDeleteCategoryResponses, AdminDeleteCategoryErrors, ThrowOnError>({
        url: '/admin/settings/{category}',
        ...options
    });
};

/**
 * Get Llm Credentials
 * List the LLM credential pool, ordered by priority, secrets masked.
 */
export const adminListLlmCredentials = <ThrowOnError extends boolean = false>(options?: Options<AdminListLlmCredentialsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<AdminListLlmCredentialsResponses, AdminListLlmCredentialsErrors, ThrowOnError>({
        url: '/admin/llm-credentials',
        ...options
    });
};

/**
 * Post Llm Credential
 * Append a new credential to the end of the priority order.
 */
export const adminCreateLlmCredential = <ThrowOnError extends boolean = false>(options: Options<AdminCreateLlmCredentialData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AdminCreateLlmCredentialResponses, AdminCreateLlmCredentialErrors, ThrowOnError>({
        url: '/admin/llm-credentials',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Delete Llm Credential Route
 */
export const adminDeleteLlmCredential = <ThrowOnError extends boolean = false>(options: Options<AdminDeleteLlmCredentialData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<AdminDeleteLlmCredentialResponses, AdminDeleteLlmCredentialErrors, ThrowOnError>({
        url: '/admin/llm-credentials/{credential_id}',
        ...options
    });
};

/**
 * Put Llm Credential
 * Update a credential. An empty ``secret`` leaves the stored value unchanged.
 */
export const adminUpdateLlmCredential = <ThrowOnError extends boolean = false>(options: Options<AdminUpdateLlmCredentialData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<AdminUpdateLlmCredentialResponses, AdminUpdateLlmCredentialErrors, ThrowOnError>({
        url: '/admin/llm-credentials/{credential_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Post Reorder Llm Credentials
 * Reassign priority 1..N following the given order.
 *
 * ``ordered_ids`` must contain exactly the existing credential ids.
 */
export const adminReorderLlmCredentials = <ThrowOnError extends boolean = false>(options: Options<AdminReorderLlmCredentialsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AdminReorderLlmCredentialsResponses, AdminReorderLlmCredentialsErrors, ThrowOnError>({
        url: '/admin/llm-credentials/reorder',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Admin Github Manifest
 * Generate a GitHub App manifest and CSRF state for the manifest flow.
 *
 * ``installation`` is not a subscribable event — GitHub delivers installation
 * lifecycle payloads to every App automatically, and including it in
 * ``default_events`` makes the manifest invalid.
 *
 * GitHub requires ``hook_attributes.url`` to be a publicly reachable URL.
 * In dev the backend is usually on localhost, so the caller must pass
 * ``public_url`` pointing at a tunnel (ngrok, cloudflared, etc). That URL
 * is also used for ``redirect_url`` so the manifest conversion callback
 * actually hits *this* backend.
 *
 * ``account_type`` selects which GitHub URL the user is redirected to:
 * personal settings or an organization's settings (requires ``org_slug``).
 *
 * Homepage, setup, OAuth-callback, and webhook URLs are all derived from
 * the backend's ``frontend_url`` config and known backend routes — not
 * admin-overridable. A misconfigured URL would silently break OAuth/webhook
 * delivery, so we keep them authoritative.
 */
export const adminGithubManifest = <ThrowOnError extends boolean = false>(options?: Options<AdminGithubManifestData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<AdminGithubManifestResponses, AdminGithubManifestErrors, ThrowOnError>({
        url: '/admin/github/manifest',
        ...options
    });
};

/**
 * Admin Github Manifest Callback
 * Exchange the manifest ``code`` for GitHub App credentials.
 *
 * GitHub redirects the user's browser here after they confirm the manifest.
 * Intentionally NOT gated by ``require_admin`` — when the admin typed a
 * public tunnel URL (ngrok etc.) as ``public_url``, that host is a
 * different origin than where the admin session cookie was set, so the
 * cookie isn't sent on this top-level redirect. Security is instead
 * enforced by the one-shot CSRF ``state`` token: it was generated inside
 * the admin-gated ``/admin/github/manifest`` endpoint, stored in Redis
 * with a 10-minute TTL, and is DEL'd here on first use. An attacker
 * without access to the admin session cannot produce a valid state.
 */
export const adminGithubManifestCallback = <ThrowOnError extends boolean = false>(options: Options<AdminGithubManifestCallbackData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<AdminGithubManifestCallbackResponses, AdminGithubManifestCallbackErrors, ThrowOnError>({
        url: '/admin/github/manifest/callback',
        ...options
    });
};

/**
 * Github Login
 * Initiate GitHub OAuth login with PKCE.
 */
export const loginGithub = <ThrowOnError extends boolean = false>(options?: Options<LoginGithubData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<unknown, unknown, ThrowOnError>({
        url: '/auth/github/authorize',
        ...options
    });
};

/**
 * Gitlab Login
 * Initiate GitLab OAuth login with PKCE.
 */
export const loginGitlab = <ThrowOnError extends boolean = false>(options?: Options<LoginGitlabData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<unknown, unknown, ThrowOnError>({
        url: '/auth/gitlab/authorize',
        ...options
    });
};

/**
 * Oauth Callback
 * Handle OAuth callback from any provider.
 */
export const oauthCallback = <ThrowOnError extends boolean = false>(options: Options<OauthCallbackData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<unknown, OauthCallbackErrors, ThrowOnError>({
        url: '/auth/callback/{provider}',
        ...options
    });
};

/**
 * Get Current User Profile
 * Get current authenticated user profile.
 */
export const getMe = <ThrowOnError extends boolean = false>(options?: Options<GetMeData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<GetMeResponses, GetMeErrors, ThrowOnError>({
        url: '/auth/me',
        ...options
    });
};

/**
 * Update Profile
 * Update current user's profile.
 */
export const updateProfile = <ThrowOnError extends boolean = false>(options: Options<UpdateProfileData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateProfileResponses, UpdateProfileErrors, ThrowOnError>({
        url: '/auth/me',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Logout
 * Logout — delete Redis session and clear cookie.
 */
export const logout = <ThrowOnError extends boolean = false>(options?: Options<LogoutData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).post<LogoutResponses, unknown, ThrowOnError>({
        url: '/auth/logout',
        ...options
    });
};

/**
 * Link Provider
 * Initiate OAuth flow to link an additional provider to the current user.
 */
export const linkProvider = <ThrowOnError extends boolean = false>(options: Options<LinkProviderData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<unknown, LinkProviderErrors, ThrowOnError>({
        url: '/auth/link/{provider}',
        ...options
    });
};

/**
 * Disconnect Provider
 * Disconnect a provider from the user's account (min 1 remaining).
 */
export const disconnectProvider = <ThrowOnError extends boolean = false>(options: Options<DisconnectProviderData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DisconnectProviderResponses, DisconnectProviderErrors, ThrowOnError>({
        url: '/auth/providers/{provider}',
        ...options
    });
};

/**
 * Get App Config
 * Return application configuration for the frontend.
 *
 * No authentication required — this is public configuration only
 * (provider availability, install URLs, setup-needed flag).
 */
export const getAppConfig = <ThrowOnError extends boolean = false>(options?: Options<GetAppConfigData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<GetAppConfigResponses, unknown, ThrowOnError>({
        url: '/config',
        ...options
    });
};

/**
 * List Mcp Servers
 */
export const listMcpServers = <ThrowOnError extends boolean = false>(options: Options<ListMcpServersData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListMcpServersResponses, ListMcpServersErrors, ThrowOnError>({
        url: '/mcp-servers',
        ...options
    });
};

/**
 * Create Mcp Server
 * Register a remote MCP server for an org.
 *
 * Only meaningful for git orgs (github/gitlab) — Jeanclode dispatches MCP
 * connections alongside the git-provider-scoped agent run, same as skills.
 */
export const createMcpServer = <ThrowOnError extends boolean = false>(options: Options<CreateMcpServerData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateMcpServerResponses, CreateMcpServerErrors, ThrowOnError>({
        url: '/mcp-servers',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Delete Mcp Server
 */
export const deleteMcpServer = <ThrowOnError extends boolean = false>(options: Options<DeleteMcpServerData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteMcpServerResponses, DeleteMcpServerErrors, ThrowOnError>({
        url: '/mcp-servers/{mcp_server_id}',
        ...options
    });
};

/**
 * Update Mcp Server
 */
export const updateMcpServer = <ThrowOnError extends boolean = false>(options: Options<UpdateMcpServerData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateMcpServerResponses, UpdateMcpServerErrors, ThrowOnError>({
        url: '/mcp-servers/{mcp_server_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Delete Credential
 */
export const deleteCredential = <ThrowOnError extends boolean = false>(options: Options<DeleteCredentialData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteCredentialResponses, DeleteCredentialErrors, ThrowOnError>({
        url: '/credentials',
        ...options
    });
};

/**
 * Get Credential
 * Status only — the secret itself is never returned.
 */
export const getCredential = <ThrowOnError extends boolean = false>(options: Options<GetCredentialData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetCredentialResponses, GetCredentialErrors, ThrowOnError>({
        url: '/credentials',
        ...options
    });
};

/**
 * Write Credential
 * Set or replace the credential for a subject. Token in, nothing back.
 *
 * Full-replace by design — the frontend never has the previous secret to
 * merge against (``GET`` omits it), so a partial update would silently
 * have to guess at missing fields.
 */
export const writeCredential = <ThrowOnError extends boolean = false>(options: Options<WriteCredentialData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<WriteCredentialResponses, WriteCredentialErrors, ThrowOnError>({
        url: '/credentials',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Cancel Execution
 * Cancel an execution — SCHEDULED (ADR-010), or RUNNING.
 *
 * SCHEDULED: nothing has been dispatched yet, so this is a single
 * conditional status update, no container involved.
 *
 * RUNNING: the DB row is cancelled the same conditional way, then the
 * actual container/Job is killed best-effort via
 * ``ContainerBackend.stop_container`` and unregistered from the watcher's
 * active-execution set — so a completion the container reports in the
 * meantime is ignored downstream rather than clobbering the cancellation.
 *
 * QUEUED is deliberately excluded, not just not-yet-supported: it has no
 * container yet, and dispatch consuming its own event off the stream can
 * lag admission by up to the redispatch/poll interval. Cancelling during
 * that window would flip the row to CANCELLED while dispatch, unaware,
 * goes on to launch the container anyway a moment later — orphaning it
 * with nothing left watching to kill it. Cancel becomes possible the
 * moment the row flips to RUNNING, once a container actually exists.
 *
 * Both conditional updates guard the same race: the redispatch poller (for
 * SCHEDULED) or the watcher's own exit handling (for RUNNING) could reach
 * a terminal state at the same instant — losing that race is reported as
 * 409, not silent success.
 */
export const cancelExecution = <ThrowOnError extends boolean = false>(options: Options<CancelExecutionData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CancelExecutionResponses, CancelExecutionErrors, ThrowOnError>({
        url: '/executions/{execution_id}/cancel',
        ...options
    });
};

/**
 * View Memory
 * View a file's raw content, or list a directory (2 levels deep).
 *
 * An empty workspace's root view returns an empty listing, not an error.
 */
export const viewMemory = <ThrowOnError extends boolean = false>(options?: Options<ViewMemoryData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ViewMemoryResponses, ViewMemoryErrors, ThrowOnError>({
        url: '/internal/memory/view',
        ...options
    });
};

/**
 * Create Memory Entry
 * Create a new memory entry.
 *
 * Rejects a path that would leave a file and a "directory" (another
 * entry's path prefix) sharing the same name — otherwise a later view on
 * the file would silently hide entries stored under it.
 */
export const createMemoryEntry = <ThrowOnError extends boolean = false>(options: Options<CreateMemoryEntryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateMemoryEntryResponses, CreateMemoryEntryErrors, ThrowOnError>({
        url: '/internal/memory/create',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Str Replace Memory Entry
 * Replace a unique substring in a file's content.
 */
export const strReplaceMemoryEntry = <ThrowOnError extends boolean = false>(options: Options<StrReplaceMemoryEntryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<StrReplaceMemoryEntryResponses, StrReplaceMemoryEntryErrors, ThrowOnError>({
        url: '/internal/memory/str_replace',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Insert Memory Entry
 * Insert a line of text after insert_line (0 = beginning of file).
 */
export const insertMemoryEntry = <ThrowOnError extends boolean = false>(options: Options<InsertMemoryEntryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<InsertMemoryEntryResponses, InsertMemoryEntryErrors, ThrowOnError>({
        url: '/internal/memory/insert',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Delete Memory Entry
 * Delete a file, or everything under a path prefix. Rejects the root.
 */
export const deleteMemoryEntry = <ThrowOnError extends boolean = false>(options: Options<DeleteMemoryEntryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DeleteMemoryEntryResponses, DeleteMemoryEntryErrors, ThrowOnError>({
        url: '/internal/memory/delete',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Rename Memory Entry
 * Rename/move a file or directory. Errors if the destination exists.
 */
export const renameMemoryEntry = <ThrowOnError extends boolean = false>(options: Options<RenameMemoryEntryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<RenameMemoryEntryResponses, RenameMemoryEntryErrors, ThrowOnError>({
        url: '/internal/memory/rename',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List Issues
 * List issues for a workspace with filters and pagination.
 */
export const listIssues = <ThrowOnError extends boolean = false>(options: Options<ListIssuesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListIssuesResponses, ListIssuesErrors, ThrowOnError>({
        url: '/issues',
        ...options
    });
};

/**
 * List Issue Repositories
 * One page of distinct repositories/projects that have issues, for the repo filter dropdown.
 *
 * Paginated and searchable server-side so the dropdown stays responsive for
 * workspaces with many repositories — callers page through until
 * ``has_more`` is false.
 */
export const listIssueRepositories = <ThrowOnError extends boolean = false>(options: Options<ListIssueRepositoriesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListIssueRepositoriesResponses, ListIssueRepositoriesErrors, ThrowOnError>({
        url: '/issues/repositories',
        ...options
    });
};

/**
 * Get Issue
 * Get issue detail.
 */
export const getIssue = <ThrowOnError extends boolean = false>(options: Options<GetIssueData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetIssueResponses, GetIssueErrors, ThrowOnError>({
        url: '/issues/{issue_id}',
        ...options
    });
};

/**
 * Retry Issue
 * Retry a failed issue — sets triage_result back to actionable.
 */
export const retryIssue = <ThrowOnError extends boolean = false>(options: Options<RetryIssueData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<RetryIssueResponses, RetryIssueErrors, ThrowOnError>({
        url: '/issues/{issue_id}/retry',
        ...options
    });
};

/**
 * Dismiss Issue
 * Dismiss an issue by setting triage_result to NOT_ACTIONABLE.
 */
export const dismissIssue = <ThrowOnError extends boolean = false>(options: Options<DismissIssueData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<DismissIssueResponses, DismissIssueErrors, ThrowOnError>({
        url: '/issues/{issue_id}/dismiss',
        ...options
    });
};

/**
 * Fix Issue
 * Manually trigger a fix for an issue — launches a single CLI container.
 */
export const fixIssue = <ThrowOnError extends boolean = false>(options: Options<FixIssueData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<FixIssueResponses, FixIssueErrors, ThrowOnError>({
        url: '/issues/{issue_id}/fix',
        ...options
    });
};

/**
 * Resolve Issue
 * Manually trigger an issue-resolve run for a GitHub/GitLab issue.
 */
export const resolveIssue = <ThrowOnError extends boolean = false>(options: Options<ResolveIssueData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<ResolveIssueResponses, ResolveIssueErrors, ThrowOnError>({
        url: '/issues/{issue_id}/resolve',
        ...options
    });
};

/**
 * Get Plugins Overview
 * Return marketplaces (with manifests inlined) and installed plugins.
 *
 * Three steps rather than one: read what we need, fetch every manifest with
 * no session open, then write the sync results back. Fetching a manifest is
 * a call out to GitHub or GitLab, so doing it inside a session would keep a
 * pooled connection checked out for the length of that round trip — once per
 * marketplace, on a page the whole team loads.
 */
export const getPluginsOverview = <ThrowOnError extends boolean = false>(options: Options<GetPluginsOverviewData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetPluginsOverviewResponses, GetPluginsOverviewErrors, ThrowOnError>({
        url: '/plugins',
        ...options
    });
};

/**
 * Install Plugins
 * Install one or more plugins from a connected marketplace.
 */
export const installPlugins = <ThrowOnError extends boolean = false>(options: Options<InstallPluginsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<InstallPluginsResponses, InstallPluginsErrors, ThrowOnError>({
        url: '/plugins',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Uninstall Plugin
 * Uninstall a plugin.
 */
export const uninstallPlugin = <ThrowOnError extends boolean = false>(options: Options<UninstallPluginData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<UninstallPluginResponses, UninstallPluginErrors, ThrowOnError>({
        url: '/plugins/{install_id}',
        ...options
    });
};

/**
 * Update Installed Plugin
 * Patch settings on an installed plugin.
 */
export const updateInstalledPlugin = <ThrowOnError extends boolean = false>(options: Options<UpdateInstalledPluginData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateInstalledPluginResponses, UpdateInstalledPluginErrors, ThrowOnError>({
        url: '/plugins/{install_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Connect Marketplace
 * Connect a marketplace by fetching and validating its manifest.
 */
export const connectMarketplace = <ThrowOnError extends boolean = false>(options: Options<ConnectMarketplaceData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<ConnectMarketplaceResponses, ConnectMarketplaceErrors, ThrowOnError>({
        url: '/marketplaces',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Disconnect Marketplace
 * Disconnect a marketplace and uninstall every plugin sourced from it.
 */
export const disconnectMarketplace = <ThrowOnError extends boolean = false>(options: Options<DisconnectMarketplaceData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DisconnectMarketplaceResponses, DisconnectMarketplaceErrors, ThrowOnError>({
        url: '/marketplaces/{marketplace_id}',
        ...options
    });
};

/**
 * List Organizations
 * List all organizations in a workspace, optionally filtered by provider.
 *
 * Supports multiple values: ``?provider=github&provider=gitlab``
 */
export const listOrganizations = <ThrowOnError extends boolean = false>(options: Options<ListOrganizationsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListOrganizationsResponses, ListOrganizationsErrors, ThrowOnError>({
        url: '/organizations',
        ...options
    });
};

/**
 * Claim Organization
 * Claim an orphaned organization into a workspace.
 *
 * After a GitHub App installation, the webhook creates an Organization with
 * no workspace. The frontend calls this to assign it to the user's
 * current workspace.
 */
export const claimOrganization = <ThrowOnError extends boolean = false>(options: Options<ClaimOrganizationData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<ClaimOrganizationResponses, ClaimOrganizationErrors, ThrowOnError>({
        url: '/organizations/claim',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Delete Organization
 * Delete an organization and uninstall from the provider if applicable.
 */
export const deleteOrganization = <ThrowOnError extends boolean = false>(options: Options<DeleteOrganizationData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteOrganizationResponses, DeleteOrganizationErrors, ThrowOnError>({
        url: '/organizations/{org_id}',
        ...options
    });
};

/**
 * Get Organization
 * Get organization details.
 */
export const getOrganization = <ThrowOnError extends boolean = false>(options: Options<GetOrganizationData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetOrganizationResponses, GetOrganizationErrors, ThrowOnError>({
        url: '/organizations/{org_id}',
        ...options
    });
};

/**
 * Update Organization
 * Update an organization.
 */
export const updateOrganization = <ThrowOnError extends boolean = false>(options: Options<UpdateOrganizationData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateOrganizationResponses, UpdateOrganizationErrors, ThrowOnError>({
        url: '/organizations/{org_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Sync Organization Repositories
 * Re-scan the provider for repositories this org should have.
 *
 * Webhooks are the normal path, but a dropped delivery — or a GitLab
 * instance with no system hook configured — leaves projects the org owns
 * with no row at all. This runs the same sync the connection ran, which
 * creates what is missing, repairs what is stale, and backfills open
 * PRs/MRs and issues.
 */
export const syncOrganizationRepositories = <ThrowOnError extends boolean = false>(options: Options<SyncOrganizationRepositoriesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<SyncOrganizationRepositoriesResponses, SyncOrganizationRepositoriesErrors, ThrowOnError>({
        url: '/organizations/{org_id}/sync-repositories',
        ...options
    });
};

/**
 * List Organization Members
 * List the org's provider members — the candidates for the notify picker.
 *
 * Not the same set as ``list_workspace_members``, which only returns
 * people who have logged into Jeanclode. ``sync_org_members``
 * pre-populates an identity per provider member, so the maintainer who
 * has never opened the dashboard is still selectable here.
 */
export const listOrganizationMembers = <ThrowOnError extends boolean = false>(options: Options<ListOrganizationMembersData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListOrganizationMembersResponses, ListOrganizationMembersErrors, ThrowOnError>({
        url: '/organizations/{org_id}/members',
        ...options
    });
};

/**
 * List Organization Subgroups
 * Subgroups below this org, with their direct repo counts.
 *
 * Feeds the subgroup-pack exclusion picker: only the connected group is
 * configurable, so this is where the tenant sees which subgroups exist and
 * how big each one is before deciding which to leave out.
 */
export const listOrganizationSubgroups = <ThrowOnError extends boolean = false>(options: Options<ListOrganizationSubgroupsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListOrganizationSubgroupsResponses, ListOrganizationSubgroupsErrors, ThrowOnError>({
        url: '/organizations/{org_id}/subgroups',
        ...options
    });
};

/**
 * Get Organization Settings
 * Get settings for an organization (fills defaults for missing keys).
 */
export const getOrganizationSettings = <ThrowOnError extends boolean = false>(options: Options<GetOrganizationSettingsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetOrganizationSettingsResponses, GetOrganizationSettingsErrors, ThrowOnError>({
        url: '/organizations/{org_id}/settings',
        ...options
    });
};

/**
 * Update Organization Settings
 * Update settings for an organization.
 *
 * Merges the request into the stored settings rather than replacing them,
 * so a caller that sends one field doesn't reset the rest to their
 * defaults.
 */
export const updateOrganizationSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateOrganizationSettingsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateOrganizationSettingsResponses, UpdateOrganizationSettingsErrors, ThrowOnError>({
        url: '/organizations/{org_id}/settings',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Get Stats
 * Get per-organization stats for the dashboard.
 */
export const getOrganizationStats = <ThrowOnError extends boolean = false>(options: Options<GetOrganizationStatsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetOrganizationStatsResponses, GetOrganizationStatsErrors, ThrowOnError>({
        url: '/organizations/{org_id}/stats',
        ...options
    });
};

/**
 * List Pull Requests
 * List pull requests for a workspace.
 */
export const listPullRequests = <ThrowOnError extends boolean = false>(options: Options<ListPullRequestsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListPullRequestsResponses, ListPullRequestsErrors, ThrowOnError>({
        url: '/workspaces/{workspace_id}/pull-requests',
        ...options
    });
};

/**
 * List Pull Request Repositories
 * One page of distinct repositories that have pull requests, for the repo filter dropdown.
 *
 * Paginated and searchable server-side, mirroring ``GET /issues/repositories``.
 */
export const listPullRequestRepositories = <ThrowOnError extends boolean = false>(options: Options<ListPullRequestRepositoriesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListPullRequestRepositoriesResponses, ListPullRequestRepositoriesErrors, ThrowOnError>({
        url: '/workspaces/{workspace_id}/pull-requests/repositories',
        ...options
    });
};

/**
 * Trigger Pr Workflow
 * Manually trigger a REVIEW or SUMMARY workflow on a PR.
 *
 * Only the PR's author may trigger manually — the gate compares the
 * PR's stored ``author`` (the GitHub/GitLab login at PR creation time)
 * against the current user's identity username for that provider.
 */
export const triggerPullRequestWorkflow = <ThrowOnError extends boolean = false>(options: Options<TriggerPullRequestWorkflowData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<TriggerPullRequestWorkflowResponses, TriggerPullRequestWorkflowErrors, ThrowOnError>({
        url: '/pull-requests/{pr_id}/{workflow}',
        ...options
    });
};

/**
 * List Repos
 * One page of an organization's repositories, name-ordered.
 *
 * Paginated server-side so groups with hundreds of repos stay responsive —
 * callers that genuinely need every repo page through until ``has_more`` is
 * false.
 */
export const listRepos = <ThrowOnError extends boolean = false>(options: Options<ListReposData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListReposResponses, ListReposErrors, ThrowOnError>({
        url: '/repos',
        ...options
    });
};

/**
 * Lookup Repos
 * Resolve repository ids to names, for settings that store ids.
 *
 * The always-include list holds ids so a rename can't stale it out, which
 * leaves the picker with nothing to render until they're resolved. Only
 * repos in an org the caller can reach come back; an id they can't see is
 * dropped rather than refused, so one stale entry can't blank the picker.
 */
export const lookupRepos = <ThrowOnError extends boolean = false>(options?: Options<LookupReposData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<LookupReposResponses, LookupReposErrors, ThrowOnError>({
        url: '/repos/lookup',
        ...options
    });
};

/**
 * Bulk Update Repo Settings
 * Enable or disable triggers on every repository of an organization.
 *
 * ``search`` narrows the set to exactly what the dashboard's repo filter is
 * showing, so the bulk toggle never reaches repos the user can't see.
 */
export const bulkUpdateRepoSettings = <ThrowOnError extends boolean = false>(options: Options<BulkUpdateRepoSettingsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<BulkUpdateRepoSettingsResponses, BulkUpdateRepoSettingsErrors, ThrowOnError>({
        url: '/repos/settings',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Update Repo Settings
 * Update settings for a repository (e.g. enable/disable triggers).
 */
export const updateRepoSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateRepoSettingsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateRepoSettingsResponses, UpdateRepoSettingsErrors, ThrowOnError>({
        url: '/repos/{repo_id}/settings',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Link Related Repo
 * Group two GitHub/GitLab repos together — both get cloned into the same
 * workspace whenever either one is worked on. Manual only: unlike Sentry
 * project mapping, there's no auto-resolution signal for "these repos
 * belong together".
 *
 * The two repos may sit in different git orgs when both are GitLab repos in
 * the same workspace (see ``db_link_repos``); the caller must have access to
 * both orgs.
 */
export const linkRelatedRepo = <ThrowOnError extends boolean = false>(options: Options<LinkRelatedRepoData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<LinkRelatedRepoResponses, LinkRelatedRepoErrors, ThrowOnError>({
        url: '/repos/{repo_id}/related',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Unlink Related Repo
 * Remove a repo-group link between two repos.
 */
export const unlinkRelatedRepo = <ThrowOnError extends boolean = false>(options: Options<UnlinkRelatedRepoData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<UnlinkRelatedRepoResponses, UnlinkRelatedRepoErrors, ThrowOnError>({
        url: '/repos/{repo_id}/related/{related_repo_id}',
        ...options
    });
};

/**
 * Add Gitlab Source
 * Add a GitLab source by providing a Group or Project Access Token.
 *
 * This endpoint:
 * 1. Verifies the GitLab token is valid
 * 2. Determines if it's a group or project token
 * 3. Creates Organization (+ single repo for project tokens) with encrypted token
 * 4. Queues background repo sync (group tokens only)
 */
export const addGitlabSource = <ThrowOnError extends boolean = false>(options: Options<AddGitlabSourceData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AddGitlabSourceResponses, AddGitlabSourceErrors, ThrowOnError>({
        url: '/sources/gitlab',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Link Sentry Source
 * Link a Sentry organization to a workspace.
 *
 * The Organization (provider=sentry) is created automatically when Sentry
 * sends an ``installation.created`` webhook.  This endpoint links it to a
 * workspace so that project sync and issue processing can begin.
 *
 * Flow:
 * 1. Calls Sentry API with the provided auth_token to discover the org slug
 * 2. Finds the webhook-created Organization by slug (or creates one)
 * 3. Encrypts and stores the auth_token on the Organization
 * 4. Links it to the provided workspace
 * 5. Stores the requested backfill scope, which bounds how much existing
 * Sentry history the chained backfill imports
 * 6. Queues background project sync
 */
export const linkSentrySource = <ThrowOnError extends boolean = false>(options: Options<LinkSentrySourceData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<LinkSentrySourceResponses, LinkSentrySourceErrors, ThrowOnError>({
        url: '/sources/sentry',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * List Source Projects
 * List synced source projects for an organization.
 */
export const listSourceProjects = <ThrowOnError extends boolean = false>(options: Options<ListSourceProjectsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListSourceProjectsResponses, ListSourceProjectsErrors, ThrowOnError>({
        url: '/sources/sentry/projects',
        ...options
    });
};

/**
 * Resolve Mappings
 * Queue auto-resolution of project-to-repo mappings.
 *
 * Publishes a single job per Sentry org. The consumer unions all git
 * orgs in the same workspace, fetches Sentry code mappings once, and
 * falls back to fuzzy name matching across the combined repo pool.
 */
export const resolveProjectMappings = <ThrowOnError extends boolean = false>(options: Options<ResolveProjectMappingsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<ResolveProjectMappingsResponses, ResolveProjectMappingsErrors, ThrowOnError>({
        url: '/sources/sentry/projects/resolve',
        ...options
    });
};

/**
 * Update Project
 * Manually set or clear the repo mapping for a source project.
 *
 * Sets mapping_method to 'manual'. Manual mappings are never
 * overwritten by auto-resolution.
 */
export const updateSourceProject = <ThrowOnError extends boolean = false>(options: Options<UpdateSourceProjectData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateSourceProjectResponses, UpdateSourceProjectErrors, ThrowOnError>({
        url: '/sources/sentry/projects/{project_id}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Trigger Backfill
 * Manually trigger issue backfill for a Sentry org.
 *
 * Queues a background task that fetches unresolved issues from the Sentry
 * API for each mapped project and creates Issue records with
 * status=PENDING.
 *
 * The run carries an explicit scope, so it proceeds even for an org whose
 * stored preference is ``none`` — skipping the import at onboarding is a
 * deferral, not a permanent opt-out.
 */
export const triggerBackfill = <ThrowOnError extends boolean = false>(options: Options<TriggerBackfillData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<TriggerBackfillResponses, TriggerBackfillErrors, ThrowOnError>({
        url: '/sources/sentry/backfill',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Stream real-time events for a workspace
 * Single SSE connection scoped to a workspace. Only events for the given workspace are delivered.
 */
export const streamEvents = <ThrowOnError extends boolean = false>(options: Options<StreamEventsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<StreamEventsResponses, StreamEventsErrors, ThrowOnError>({
        url: '/stream',
        ...options
    });
};

/**
 * Sentry Webhook
 * Receive a Sentry webhook, verify signature, publish to Redis Stream.
 *
 * Routes installation webhooks to a separate stream for onboarding flow.
 * All other webhooks go to the main issue processing stream.
 *
 * This is the Stage 1 hot path per ADR-004: validate and queue.
 */
export const sentryWebhookWebhooksSentryPost = <ThrowOnError extends boolean = false>(options: Options<SentryWebhookWebhooksSentryPostData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<SentryWebhookWebhooksSentryPostResponses, SentryWebhookWebhooksSentryPostErrors, ThrowOnError>({
        url: '/webhooks/sentry',
        ...options
    });
};

/**
 * Github Webhook
 * Unified GitHub webhook router — routes by X-GitHub-Event header.
 */
export const githubWebhook = <ThrowOnError extends boolean = false>(options: Options<GithubWebhookData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<GithubWebhookResponses, GithubWebhookErrors, ThrowOnError>({
        url: '/webhooks/github',
        ...options
    });
};

/**
 * Gitlab Webhook
 * Receive a GitLab webhook, verify token, queue supported events.
 */
export const gitlabWebhook = <ThrowOnError extends boolean = false>(options?: Options<GitlabWebhookData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).post<GitlabWebhookResponses, GitlabWebhookErrors, ThrowOnError>({
        url: '/webhooks/gitlab',
        ...options
    });
};

/**
 * List Workspaces
 * List all workspaces the current user is a member of.
 */
export const listWorkspaces = <ThrowOnError extends boolean = false>(options?: Options<ListWorkspacesData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ListWorkspacesResponses, ListWorkspacesErrors, ThrowOnError>({
        url: '/workspaces',
        ...options
    });
};

/**
 * Create Workspace
 * Create a new workspace and add the current user as owner.
 */
export const createWorkspace = <ThrowOnError extends boolean = false>(options: Options<CreateWorkspaceData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateWorkspaceResponses, CreateWorkspaceErrors, ThrowOnError>({
        url: '/workspaces',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Finalize Workspace Onboarding
 * Finalize onboarding — create memberships and queue membership sync.
 *
 * Called when the user clicks "Finish setup". Creates immediate owner
 * memberships for the connecting user, then queues background tasks
 * for membership sync.
 */
export const finalizeWorkspaceOnboarding = <ThrowOnError extends boolean = false>(options: Options<FinalizeWorkspaceOnboardingData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<FinalizeWorkspaceOnboardingResponses, FinalizeWorkspaceOnboardingErrors, ThrowOnError>({
        url: '/workspaces/{workspace_id}/finalize',
        ...options
    });
};

/**
 * List Workspace Members
 * List paginated workspace members.
 */
export const listWorkspaceMembers = <ThrowOnError extends boolean = false>(options: Options<ListWorkspaceMembersData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListWorkspaceMembersResponses, ListWorkspaceMembersErrors, ThrowOnError>({
        url: '/workspaces/{workspace_id}/members',
        ...options
    });
};

/**
 * Get Sources
 * Get connected sources for a workspace.
 */
export const getWorkspaceSources = <ThrowOnError extends boolean = false>(options: Options<GetWorkspaceSourcesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetWorkspaceSourcesResponses, GetWorkspaceSourcesErrors, ThrowOnError>({
        url: '/workspaces/{workspace_id}/sources',
        ...options
    });
};

/**
 * Get Stats
 * Stat cards for the dashboard, issues and PR pages.
 */
export const getWorkspaceStats = <ThrowOnError extends boolean = false>(options: Options<GetWorkspaceStatsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetWorkspaceStatsResponses, GetWorkspaceStatsErrors, ThrowOnError>({
        url: '/workspaces/{workspace_id}/stats',
        ...options
    });
};

/**
 * Get Executions
 * Get paginated executions for the dashboard grid.
 */
export const getWorkspaceExecutions = <ThrowOnError extends boolean = false>(options: Options<GetWorkspaceExecutionsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetWorkspaceExecutionsResponses, GetWorkspaceExecutionsErrors, ThrowOnError>({
        url: '/workspaces/{workspace_id}/executions',
        ...options
    });
};

/**
 * Get Active Executions
 * Running then queued executions for the dashboard, in one query.
 */
export const getWorkspaceActiveExecutions = <ThrowOnError extends boolean = false>(options: Options<GetWorkspaceActiveExecutionsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetWorkspaceActiveExecutionsResponses, GetWorkspaceActiveExecutionsErrors, ThrowOnError>({
        url: '/workspaces/{workspace_id}/executions/active',
        ...options
    });
};

/**
 * Get Leaderboards
 * Get all leaderboards for the dashboard.
 */
export const getWorkspaceLeaderboards = <ThrowOnError extends boolean = false>(options: Options<GetWorkspaceLeaderboardsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetWorkspaceLeaderboardsResponses, GetWorkspaceLeaderboardsErrors, ThrowOnError>({
        url: '/workspaces/{workspace_id}/leaderboards',
        ...options
    });
};
